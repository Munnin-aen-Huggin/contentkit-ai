---
phase: 07-email-automation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - webhook/netlify/functions/ls-webhook.js
autonomous: false

user_setup:
  - service: kit
    why: "v4 API key needed for custom field updates"
    env_vars:
      - name: KIT_API_KEY
        source: "Kit Dashboard -> Settings -> Developer -> API Keys (generate a v4 API key, NOT the v3 api_secret)"
    dashboard_config:
      - task: "Create 3 custom fields: product_tier, purchase_value, purchase_date"
        location: "Kit Dashboard -> Subscribers -> click any subscriber -> Custom Fields -> + Add a Custom Field"
      - task: "Create 2 automation rules for buyer suppression"
        location: "Kit Dashboard -> Automate -> Rules"
  - service: netlify
    why: "New environment variable for v4 API key"
    env_vars:
      - name: KIT_API_KEY
        source: "From Kit Developer settings (generated above)"

must_haves:
  truths:
    - "A buyer tagged purchased-starter is removed from the lead-magnet-delivery nurture sequence"
    - "A buyer tagged purchased-full-kit is removed from the lead-magnet-delivery nurture sequence"
    - "Kit custom fields product_tier, purchase_value, and purchase_date are populated on a buyer's subscriber record after a webhook-triggered purchase"
  artifacts:
    - path: "webhook/netlify/functions/ls-webhook.js"
      provides: "Extended webhook with v4 custom field API calls"
      contains: "api.kit.com/v4/subscribers"
  key_links:
    - from: "webhook/netlify/functions/ls-webhook.js"
      to: "Kit v4 API"
      via: "fetch to api.kit.com/v4/subscribers"
      pattern: "api\\.kit\\.com/v4/subscribers"
    - from: "Kit Automation Rule (purchased-starter)"
      to: "lead-magnet-delivery sequence"
      via: "Tag added -> Unsubscribe from sequence"
      pattern: "manual Kit UI verification"
    - from: "Kit Automation Rule (purchased-full-kit)"
      to: "lead-magnet-delivery sequence"
      via: "Tag added -> Unsubscribe from sequence"
      pattern: "manual Kit UI verification"
---

<objective>
Configure buyer suppression from the nurture sequence and extend the Netlify webhook to populate Kit custom fields on purchase.

Purpose: Buyers must stop receiving nurture/upsell emails immediately upon purchase (EMAIL-01), and purchase metadata must be recorded on their Kit subscriber record for segmentation and reporting (EMAIL-06).

Output: Two Kit automation rules (buyer suppression), three Kit custom fields created, webhook updated with v4 API custom field calls, KIT_API_KEY env var added to Netlify.
</objective>

<execution_context>
@/Users/wilfredshammah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilfredshammah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-email-automation/07-RESEARCH.md
@.planning/phases/03-email-automation/03-01-SUMMARY.md
@.planning/phases/03-email-automation/03-03-SUMMARY.md
@webhook/netlify/functions/ls-webhook.js
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create Kit custom fields + automation rules + add KIT_API_KEY to Netlify</name>
  <action>
    Complete the following steps in Kit and Netlify dashboards. These cannot be automated because they require authenticated browser access to Kit and Netlify UIs.

    **Important context:** Kit is already on the Creator plan (upgraded during Phase 3). No plan upgrade needed.

    **Step A — Create 3 custom fields in Kit:**
    1. Go to Kit Dashboard -> Subscribers -> click any subscriber profile
    2. In the Custom Fields section, click "+ Add a Custom Field"
    3. Create field: `product_tier` (text)
    4. Create field: `purchase_value` (text)
    5. Create field: `purchase_date` (text)
    6. Verify all 3 fields appear in the subscriber profile

    **Step B — Create 2 buyer suppression automation rules:**
    1. Go to Kit Dashboard -> Automate -> Rules
    2. Create Rule 1:
       - Trigger: "Tag is added" -> select "purchased-starter"
       - Action: "Unsubscribe from a sequence" -> select "lead-magnet-delivery" (sequence ID 2665124)
    3. Create Rule 2:
       - Trigger: "Tag is added" -> select "purchased-full-kit"
       - Action: "Unsubscribe from a sequence" -> select "lead-magnet-delivery"
    4. Ensure both rules show as Active

    **Step C — Generate Kit v4 API key and add to Netlify:**
    1. Go to Kit Dashboard -> Settings -> Developer (or Account Settings -> Developer)
    2. Generate a new API Key (this is the v4 key, different from the existing v3 api_secret)
    3. Copy the key
    4. Go to Netlify Dashboard -> Site settings -> Environment variables
    5. Add new variable: `KIT_API_KEY` = [paste the v4 API key]
    6. Verify the variable appears in the list alongside existing vars (STRIPE_WEBHOOK_SECRET, KIT_API_SECRET, etc.)
  </action>
  <verify>
    Confirm:
    - 3 custom fields visible on any subscriber profile in Kit
    - 2 automation rules active in Kit -> Automate -> Rules
    - KIT_API_KEY environment variable set in Netlify dashboard
  </verify>
  <done>Kit has product_tier, purchase_value, purchase_date custom fields. Two buyer suppression rules are active. KIT_API_KEY is in Netlify env vars.</done>
  <resume-signal>Type "done" when all three steps are complete, or describe any issues encountered.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Extend webhook with v4 custom field API calls</name>
  <files>webhook/netlify/functions/ls-webhook.js</files>
  <action>
    Read the existing `webhook/netlify/functions/ls-webhook.js` first.

    Add a new async function `setKitCustomFields(buyerEmail, productTier, amountTotal, kitApiKey)` that:

    1. Looks up the subscriber ID via `GET https://api.kit.com/v4/subscribers?email_address={email}` with header `X-Kit-Api-Key: {kitApiKey}`
    2. If lookup fails or no subscriber found, logs a warning and returns (non-fatal — tagging already succeeded)
    3. Updates custom fields via `PUT https://api.kit.com/v4/subscribers/{subscriberId}` with body:
       ```json
       {
         "email_address": buyerEmail,
         "fields": {
           "product_tier": productTier,
           "purchase_value": (amountTotal / 100).toFixed(2),
           "purchase_date": new Date().toISOString().split("T")[0]
         }
       }
       ```
    4. Logs success or failure

    Then modify the main handler:
    - After the existing generic buyer tag block (section 6), add a new section 7
    - Read `KIT_API_KEY` from `process.env.KIT_API_KEY`
    - Determine `productTier` from the amount: 14700 -> "starter", 49900 -> "full-kit"
    - Call `setKitCustomFields(buyerEmail, productTier, amountTotal, kitApiKey)` in a try/catch
    - This is best-effort: log errors but still return 200 (tagging is the critical path, custom fields are supplementary)
    - Do NOT modify any existing v3 tag logic — keep it exactly as-is

    Use the code pattern from 07-RESEARCH.md. Keep the same coding style as the existing file (CommonJS require, no external deps, console.log/error for logging).
  </action>
  <verify>
    1. Run `node -c webhook/netlify/functions/ls-webhook.js` to verify syntax
    2. Visually confirm the file still has all 6 original sections intact plus the new section 7
    3. Confirm `setKitCustomFields` function exists and uses `api.kit.com/v4/subscribers`
    4. Confirm `KIT_API_KEY` is read from process.env (not hardcoded)
    5. Confirm the function is called with correct productTier mapping (14700 -> "starter", 49900 -> "full-kit")
  </verify>
  <done>ls-webhook.js extended with v4 custom field calls. Syntax valid. Existing v3 logic untouched. New code is non-fatal (logs errors, returns 200).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify buyer suppression and deploy updated webhook</name>
  <action>
    Verify that buyer suppression automation rules work correctly and deploy the updated webhook to Netlify.

    Two Kit automation rules for buyer suppression (purchased-starter and purchased-full-kit both unsubscribe from lead-magnet-delivery sequence) were configured in Task 1. The webhook was extended with v4 custom field API calls in Task 2.
  </action>
  <verify>
    **Suppression rule test (Kit UI):**
    1. In Kit, find or create a test subscriber who is currently in the lead-magnet-delivery sequence
    2. Manually add the "purchased-starter" tag to that subscriber
    3. Check the subscriber's activity log — they should be unsubscribed from lead-magnet-delivery within seconds
    4. If no test subscriber is in the sequence, verify the rules show "Active" status in Automate -> Rules

    **Webhook deployment:**
    1. Deploy the updated webhook to Netlify (push the webhook directory or use Netlify CLI)
    2. Check Netlify deploy logs for successful build
    3. Verify the function is accessible at the existing endpoint

    **Custom fields test (after deployment, optional if no real purchase to trigger):**
    - A real purchase test will happen in Phase 9. For now, confirm the webhook file syntax is valid and the function is deployed.
  </verify>
  <done>Buyer suppression rules verified (tag added -> removed from nurture sequence). Webhook deployed to Netlify with v4 custom field capability.</done>
  <resume-signal>Type "verified" if suppression rules work and webhook is deployed, or describe issues.</resume-signal>
</task>

</tasks>

<verification>
- Kit automation rules: Both rules active in Kit -> Automate -> Rules
- Kit custom fields: product_tier, purchase_value, purchase_date visible in subscriber profiles
- Webhook: `node -c webhook/netlify/functions/ls-webhook.js` passes
- Webhook: Contains `setKitCustomFields` function with v4 API calls
- Webhook: Deployed to Netlify with KIT_API_KEY env var
- Existing v3 tag logic unchanged
</verification>

<success_criteria>
1. Both buyer suppression automation rules are active in Kit
2. Three custom fields exist in Kit
3. Webhook file extended with v4 custom field calls and syntax-valid
4. KIT_API_KEY env var set in Netlify
5. Webhook deployed to Netlify
</success_criteria>

<output>
After completion, create `.planning/phases/07-email-automation/07-01-SUMMARY.md`
</output>
