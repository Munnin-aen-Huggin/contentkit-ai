---
phase: 04-front-end-build
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - src/input.css
autonomous: true

must_haves:
  truths:
    - "Visitor sees a real prompt example from the kit between What's Inside and Testimonials sections"
    - "Visitor sees a 'Who This Is NOT For' section that filters bad-fit buyers"
    - "Clicking 'Get Starter Kit' button opens Stripe checkout for $27 product"
    - "Clicking 'Get Full Kit' button opens Stripe checkout for $47 product"
    - "Sharing the page URL on Facebook/Twitter/LinkedIn shows branded title, description, and image preview"
    - "Google Fonts load without render-blocking (no @import in style block)"
    - "Footer Terms link points to terms-of-service.html, Privacy link points to privacy-policy.html, Refunds link exists"
  artifacts:
    - path: "index.html"
      provides: "Complete landing page with all 14 PAGE requirements satisfied"
      contains: "buy.stripe.com"
    - path: "src/input.css"
      provides: "dark-700 color token for Kit form input background"
      contains: "dark-700"
  key_links:
    - from: "index.html pricing section"
      to: "https://buy.stripe.com/cNidRa7JQaRMbqWaXK9ws00"
      via: "href on #starter-btn"
      pattern: "buy\\.stripe\\.com.*9ws00"
    - from: "index.html pricing section"
      to: "https://buy.stripe.com/5kQ8wQ8NUe3Y2Uq7Ly9ws01"
      via: "href on #full-btn"
      pattern: "buy\\.stripe\\.com.*9ws01"
    - from: "index.html head"
      to: "og-image.png"
      via: "og:image meta tag"
      pattern: "og:image.*og-image\\.png"
    - from: "index.html footer"
      to: "terms-of-service.html"
      via: "href on Terms link"
      pattern: "terms-of-service\\.html"
---

<objective>
Complete index.html by adding the two missing content sections (sample prompt preview, "Who This Is NOT For"), wiring live Stripe Payment Links to buy buttons, adding full OG/SEO meta tags, fixing the render-blocking Google Fonts @import, adding the dark-700 Tailwind token, and updating footer links to point to legal pages.

Purpose: Transforms the 85%-complete landing page into a fully functional sales page that can accept payments, share properly on social media, and load without render-blocking font requests.

Output: A complete `index.html` with all PAGE-01 through PAGE-14 requirements satisfied (except PAGE-12/13 which are verified in plan 04-04).
</objective>

<execution_context>
@/Users/wilfredshammah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wilfredshammah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-front-end-build/04-RESEARCH.md
@index.html
@src/input.css
@downloads/asset-manifest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing sections, wire Stripe URLs, and update footer links</name>
  <files>index.html</files>
  <action>
Make three sets of edits to index.html:

**1. Add Sample Prompt Preview section (PAGE-04):**
Insert a new section between the closing `</section>` of "What's Inside" (after line ~142) and the opening of "Testimonials" (line ~144). Use the exact HTML from the research document Pattern 3: a `py-20 px-6` section with a styled code card showing prompt FB-01 (Facebook PAS-framework ad). Include the badge "REAL EXAMPLE FROM THE KIT", heading "See a Real Prompt", descriptive paragraph, and the full prompt text with `[VARIABLE]` spans highlighted in `text-brand-500`. End with the footer line "500+ prompts like this, organized across 9 categories".

**2. Add "Who This Is NOT For" section (PAGE-08):**
Insert a new section between the FAQ closing `</section>` (after line ~287) and the Final CTA section (line ~289). Use the exact HTML from research Pattern 4: four items with red X marks (text-red-400) listing bad-fit buyers. Include the reassuring closer "If that's not you -- you're exactly who we built this for."

**3. Wire Stripe Payment Link URLs:**
Replace the Starter Kit buy button href:
- Change `<a href="#" id="starter-btn"` to `<a href="https://buy.stripe.com/cNidRa7JQaRMbqWaXK9ws00" id="starter-btn"`
Replace the Full Kit buy button href:
- Change `<a href="#" id="full-btn"` to `<a href="https://buy.stripe.com/5kQ8wQ8NUe3Y2Uq7Ly9ws01" id="full-btn"`

**4. Update footer links:**
Replace the Terms `href="#"` with `href="terms-of-service.html"`.
Replace the Privacy `href="#"` with `href="privacy-policy.html"`.
Add a Refund Policy link: `<a href="refund-policy.html" class="hover:text-white transition">Refunds</a>` between Privacy and Contact.

**5. Remove HTML comment placeholders:**
Remove the `<!-- REPLACE with your Stripe Payment Link -->` comments above both buy buttons (no longer needed).
  </action>
  <verify>
Grep index.html for:
- `buy.stripe.com` appears exactly 2 times (both buy buttons wired)
- `href="#"` does NOT appear in footer links (all wired)
- "REAL EXAMPLE FROM THE KIT" exists (PAGE-04 section present)
- "Who This Is NOT For" exists (PAGE-08 section present)
- `terms-of-service.html` exists in footer
- `privacy-policy.html` exists in footer
- `refund-policy.html` exists in footer
  </verify>
  <done>index.html has all 14 required sections visible, both buy buttons point to live Stripe Payment Links, and footer links point to legal page filenames.</done>
</task>

<task type="auto">
  <name>Task 2: Add OG/SEO meta tags and fix Google Fonts render-blocking</name>
  <files>index.html, src/input.css</files>
  <action>
**1. Fix Google Fonts render-blocking:**
In `<head>`, remove the entire `<style>` block that contains the `@import url('https://fonts.googleapis.com/...')` line AND the `body { font-family: ... }` declaration.

Replace it with two parts:
(a) Non-blocking font loading links BEFORE the `output.css` link:
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
```

(b) A `<style>` block (kept AFTER the font links) containing ONLY the custom CSS that was previously in the style block — but NOT the @import:
```html
<style>
  body { font-family: 'Inter', sans-serif; }
  .gradient-text { background: linear-gradient(135deg, #0ea5e9, #8b5cf6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .glow { box-shadow: 0 0 60px rgba(14, 165, 233, 0.15); }
  .fade-in { animation: fadeIn 0.6s ease-out; }
  @keyframes fadeIn { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
  .pulse-border { animation: pulseBorder 2s ease-in-out infinite; }
  @keyframes pulseBorder { 0%, 100% { border-color: #0ea5e9; } 50% { border-color: #8b5cf6; } }
</style>
```

**2. Add OG and Twitter meta tags:**
Insert the full OG/SEO meta block in `<head>` immediately after the existing `<meta name="description">` tag. Use the exact tags from research Pattern 2:

```html
<link rel="canonical" href="https://getcontentkit.com/">

<!-- Open Graph -->
<meta property="og:type" content="website">
<meta property="og:title" content="ContentKit AI — 500+ AI Marketing Prompts">
<meta property="og:description" content="Stop staring at a blank screen. 500+ copy-paste prompts that generate converting ads, emails, and social posts in seconds.">
<meta property="og:url" content="https://getcontentkit.com/">
<meta property="og:image" content="https://getcontentkit.com/og-image.png">
<meta property="og:image:width" content="1200">
<meta property="og:image:height" content="630">
<meta property="og:image:alt" content="ContentKit AI — 500+ AI Marketing Prompts">
<meta property="og:site_name" content="ContentKit AI">

<!-- Twitter/X Card -->
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="ContentKit AI — 500+ AI Marketing Prompts">
<meta name="twitter:description" content="Stop staring at a blank screen. 500+ copy-paste prompts that generate converting ads, emails, and social posts in seconds.">
<meta name="twitter:image" content="https://getcontentkit.com/og-image.png">
<meta name="twitter:image:alt" content="ContentKit AI — 500+ AI Marketing Prompts">
```

NOTE: `twitter:card` uses `name` attribute, NOT `property`. All OG tags use `property`.

**3. Add dark-700 token to src/input.css:**
Add `--color-dark-700: oklch(0.18 0.01 270);` to the @theme block in src/input.css, between `brand-900` and `dark-800` lines. This ensures the Kit form input field background (`bg-dark-700` class already in use) compiles correctly.
  </action>
  <verify>
Grep index.html for:
- `@import url` does NOT appear (render-blocking pattern removed)
- `rel="preconnect"` appears (font preconnect added)
- `og:title` appears (OG tags present)
- `twitter:card` appears (Twitter card present)
- `og:image` contains `og-image.png`
- `canonical` href is `https://getcontentkit.com/`

Grep src/input.css for:
- `dark-700` token exists
  </verify>
  <done>Google Fonts loads via non-blocking preconnect+link pattern. Full OG and Twitter Card meta tags are in head. dark-700 Tailwind token is defined in input.css.</done>
</task>

</tasks>

<verification>
1. Open index.html in browser — all sections visible in order: nav, hero, social proof, problem/solution, what's inside, sample prompt preview (NEW), testimonials, lead magnet opt-in, pricing, FAQ, who this is NOT for (NEW), final CTA, footer
2. Click "Get Starter Kit" button — opens Stripe checkout page for $27
3. Click "Get Full Kit" button — opens Stripe checkout page for $47
4. View page source — no `@import url` in style block, preconnect links present, full OG tags in head
5. Footer Terms/Privacy/Refunds links point to .html files (not #)
</verification>

<success_criteria>
- All 14 PAGE-* sections present in index.html
- Both buy buttons are live Stripe Payment Links
- OG meta tags complete (og:title, og:description, og:image, og:url, twitter:card)
- No render-blocking @import for Google Fonts
- Footer legal links point to real .html files
</success_criteria>

<output>
After completion, create `.planning/phases/04-front-end-build/04-01-SUMMARY.md`
</output>
